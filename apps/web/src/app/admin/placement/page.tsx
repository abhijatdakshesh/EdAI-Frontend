import PlacementDashboard from '@/features/placement/placement-dashboard';

export const metadata = { title: 'Placement Intelligence — Ed8AI' };

export default function PlacementPage() {
  return <PlacementDashboard />;
}
