import TimetableGenerator from '@/features/timetable/timetable-generator';

export const metadata = { title: 'AI Timetable Generator — Ed8AI' };

export default function TimetablePage() {
  return <TimetableGenerator />;
}
