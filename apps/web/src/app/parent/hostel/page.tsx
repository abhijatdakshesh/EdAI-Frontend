import { ParentHostel } from "@/features/parent/parent-hostel";

export const metadata = { title: "Hostel & Transport — Ed8AI" };

export default function ParentHostelPage() {
  return <ParentHostel />;
}
